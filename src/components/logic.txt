function to handle Generations

function to start game

function to pause game

function to reset game
if className === bgAlive
return className bgDead

function to test logic

if cell has 1 neighbor die
if cell has 2 neighbors die
if cell has 3 neighbors live
if cell has 4 neighbors live and create
if cell has 5 neighbors live and create
if cell has 6 neighbors die